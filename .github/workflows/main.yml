
name: CI

on:

  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: waylonwalker/actions-args@664fbfce382a6ca9e9e9cedcbd724fabc7781eb8
        with:
          version: '1.0.0'
