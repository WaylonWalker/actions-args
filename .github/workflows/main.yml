
name: CI

on:

  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: waylonwalker/actions-args@3cfd894567efd91a3683a6be1eb364a4391f7dc5
        with:
          version: '0.2.0'
